config {
  type: "view",
  schema: "staging"
}
SELECT
    unique_key,
    taxi_id,
    trip_seconds,
    trip_miles,
    fare,
    tips,
    trip_total,
    company,
    payment_type
FROM
  ${ref("taxi_trips")}
WHERE
    tips > 0
AND fare > 0
AND trip_total > 0
